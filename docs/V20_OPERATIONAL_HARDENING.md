# V20 operational hardening

V20 fixes the integration API contract, runs CI against a MongoDB replica set, removes shell interpolation from database user creation, installs and verifies MongoDB database tools, grants only SETUID/SETGID for controlled privilege dropping, completes the active credential contract, removes LMS event types and student dashboard metrics, and expands readiness checks.
