# V22 credential security

V22 owner-scopes credential retrieval and download, removes self-service trainer issuance, requires core credential fields, enforces unique credential references, aligns CSV and API fields, resolves recipients by email, wraps bulk insertion and audit in one transaction, adopts identifier-only public verification with dedicated rate limiting, and adds ownership/integrity tests.
