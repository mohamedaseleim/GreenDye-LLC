# V23 credential lifecycle and migration hardening

V23 introduces 128-bit public credential IDs, a required and verified credential-reference index migration, explicit lifecycle states, transactional creation/update/lifecycle auditing, soft deletion, removal of obsolete verification tokens, metadata contract alignment, and redacted frontend error reporting.
