# V18 remediation

V18 adopts **GreenDye for Training and Consultancy** as the sole product name, replaces live Academy defaults, clarifies that learning delivery and records remain in independent Moodle, converts certificate administration terminology to professional credentials, fixes routing and metadata, removes the duplicate locale, centralizes authenticated services further, publishes production services on loopback for a host reverse proxy, URL-encodes MongoDB credentials at runtime, refreshes ClamAV signatures before startup, and adds integration testing to CI.
