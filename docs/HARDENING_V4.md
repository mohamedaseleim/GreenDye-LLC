# GreenDye for Training and Consultancy hardening v4

Implemented: fixed route precedence, removed orphan chat client, centralized role checks, mass-assignment allowlists, verified-email enforcement, endpoint-specific rate limits, verification resend, HTML escaping, bearer-only authentication, token-version session revocation, password-change revocation, CSP/API hardening, Node engine pinning, transactional proposal creation/send/decision, transactional payment and invoice updates, validated proposal pricing and payment terms, invoice state transitions, protected project membership updates, document listing/upload/download/archive/restore/reclassification/delete, project and daily upload quotas, OOXML package validation, audit records, and React route chunking.

Operational requirements: MongoDB must run as a replica set for multi-document transactions. Configure SMTP and all environment variables from `.env.example`. Keep live gateway functionality disabled until a selected provider is integrated and its signed webhooks pass sandbox acceptance tests. Install and connect an external malware scanner such as ClamAV at the infrastructure layer for defense in depth.
