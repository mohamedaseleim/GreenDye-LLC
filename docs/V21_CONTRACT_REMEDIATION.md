# V21 contract remediation

V21 unifies certificate, trainer, and consultant verification responses; implements structured bulk-credential results; replaces obsolete verification integration tests; supports historical credential export during migration; caches readiness checks; provides a bounded ClamAV update fallback; and removes unused frontend dependencies.
