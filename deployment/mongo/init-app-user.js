const username=process.env.MONGO_APP_USERNAME;const password=process.env.MONGO_APP_PASSWORD;const database=process.env.MONGO_APP_DATABASE||'green_consulting';if(!username||!password)throw new Error('Application database credentials are required');const target=db.getSiblingDB(database);if(!target.getUser(username))target.createUser({user:username,pwd:password,roles:[{role:'readWrite',db:database}]});
