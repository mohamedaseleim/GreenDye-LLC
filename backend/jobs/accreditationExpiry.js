const Trainer=require('../models/Trainer');const Certificate=require('../models/Certificate');exports.run=async()=>{const now=new Date();const trainers=await Trainer.updateMany({'accreditations.expiryDate':{$lt:now},applicationStatus:'approved'},{$set:{applicationStatus:'expired',isVerified:false}});const certificates=await Certificate.updateMany({expiryDate:{$lt:now},isRevoked:false,isValid:true},{$set:{isValid:false}});return{trainers,certificates}};if(require.main===module){require('dotenv').config();require('../config/database')();exports.run().then(x=>{console.log(x);process.exit(0)}).catch(e=>{console.error(e);process.exit(1)})};