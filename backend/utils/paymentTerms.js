const pick=require('./pick');const {toMinor,fromMinor}=require('./money');
exports.calculatePaymentTerms=(terms,total,currency)=>{if(!Array.isArray(terms)||!terms.length)return[];const percentages=terms.map(x=>Number(x.percentage||0));if(percentages.some(x=>!Number.isFinite(x)||x<0)||Math.abs(percentages.reduce((a,b)=>a+b,0)-100)>0.01)throw Object.assign(new Error('Proposal payment terms must total 100%'),{statusCode:400});const totalMinor=toMinor(total,currency);let allocated=0;return terms.map((x,index)=>{const amountMinor=index===terms.length-1?totalMinor-allocated:Math.round(totalMinor*percentages[index]/100);allocated+=amountMinor;return{...pick(x,['label','dueEvent']),percentage:percentages[index],amountMinor,amount:fromMinor(amountMinor,currency)}})};
