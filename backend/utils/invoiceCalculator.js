exports.calculateInvoice=body=>{const items=(body.items||[]).map(i=>{const quantity=Number(i.quantity||1),unitPrice=Number(i.unitPrice);if(!i.description||quantity<=0||unitPrice<0)throw Object.assign(new Error('Invalid invoice item'),{statusCode:400});return{...i,quantity,unitPrice,amount:Number((quantity*unitPrice).toFixed(2))}});if(!items.length)throw Object.assign(new Error('Invoice requires items'),{statusCode:400});const subtotal=items.reduce((a,i)=>a+i.amount,0),taxRate=Number(body.taxRate||0),discount=Number(body.discount||0);if(taxRate<0||taxRate>100||discount<0||discount>subtotal)throw Object.assign(new Error('Invalid tax or discount'),{statusCode:400});const tax=Number((subtotal*taxRate/100).toFixed(2)),total=Number((subtotal+tax-discount).toFixed(2));return{items,subtotal,tax,discount,total,taxRate}};