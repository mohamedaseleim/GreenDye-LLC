const ConsultationService = require('../models/ConsultationService');
const ConsultationRequest = require('../models/ConsultationRequest');
exports.listServices = async (req,res,next) => { try { const data=await ConsultationService.find({isPublished:true}).sort({createdAt:-1}); res.json({success:true,data}); } catch(e){next(e);} };
exports.getService = async (req,res,next) => { try { const data=await ConsultationService.findOne({slug:req.params.slug,isPublished:true}); if(!data)return res.status(404).json({success:false,message:'Service not found'}); res.json({success:true,data}); } catch(e){next(e);} };
exports.createService = async (req,res,next) => { try { const data=await ConsultationService.create({...req.body,createdBy:req.user._id}); res.status(201).json({success:true,data}); } catch(e){next(e);} };
exports.updateService = async (req,res,next) => { try { const data=await ConsultationService.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true}); if(!data)return res.status(404).json({success:false,message:'Service not found'}); res.json({success:true,data}); } catch(e){next(e);} };
exports.submitRequest = async (req,res,next) => { try { const data=await ConsultationRequest.create({...req.body,client:req.user._id,status:'submitted'}); res.status(201).json({success:true,data}); } catch(e){next(e);} };
exports.myRequests = async (req,res,next) => { try { const data=await ConsultationRequest.find({client:req.user._id}).populate('service','name slug').sort({createdAt:-1}); res.json({success:true,data}); } catch(e){next(e);} };
exports.listRequests = async (req,res,next) => { try { const q=req.query.status?{status:req.query.status}:{}; const data=await ConsultationRequest.find(q).populate('client','name email').populate('service','name slug').sort({createdAt:-1}); res.json({success:true,data}); } catch(e){next(e);} };
exports.updateRequestStatus = async (req,res,next) => { try { const data=await ConsultationRequest.findByIdAndUpdate(req.params.id,{status:req.body.status},{new:true,runValidators:true}); if(!data)return res.status(404).json({success:false,message:'Consultation request not found'}); res.json({success:true,data}); } catch(e){next(e);} };
