const r=require('express').Router();const multer=require('multer');const {protect}=require('../middleware/auth');const {loadProject,requireProjectPermission}=require('../middleware/projectAccess');const c=require('../controllers/projectDocumentController');const allow=['application/pdf','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','image/png','image/jpeg'];const up=multer({storage:multer.memoryStorage(),limits:{fileSize:25*1024*1024},fileFilter:(q,f,cb)=>cb(allow.includes(f.mimetype)?null:new Error('File type not allowed'),allow.includes(f.mimetype))});r.post('/projects/:projectId/documents',protect,loadProject,requireProjectPermission('uploadFiles'),up.single('file'),c.upload);r.get('/projects/:projectId/documents/:documentId/download',protect,loadProject,c.download);module.exports=r;