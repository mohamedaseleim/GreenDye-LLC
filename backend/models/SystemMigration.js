const mongoose=require('mongoose');const schema=new mongoose.Schema({_id:String,status:{type:String,enum:['running','completed','failed'],required:true},owner:String,startedAt:Date,heartbeatAt:Date,completedAt:Date,checkpoint:{collection:String,lastId:String},stats:mongoose.Schema.Types.Mixed,error:String},{timestamps:true});module.exports=mongoose.model('SystemMigration',schema);
