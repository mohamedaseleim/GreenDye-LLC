/* eslint-disable no-console */
require('dotenv').config();const mongoose=require('mongoose');const Certificate=require('../models/Certificate');
(async()=>{try{await mongoose.connect(process.env.MONGODB_URI);const cursor=Certificate.collection.find({$or:[{recipientName:{$exists:false}},{credentialTitle:{$exists:false}}]});let changed=0;for await(const row of cursor){const set={};if(row.recipientName===undefined&&(row.traineeName||row.userName))set.recipientName=row.traineeName||row.userName;if(row.credentialTitle===undefined&&(row.courseTitle||row.courseName))set.credentialTitle=row.courseTitle||(typeof row.courseName==='string'?row.courseName:row.courseName?.en);if(Object.keys(set).length){await Certificate.collection.updateOne({_id:row._id},{$set:set});changed++}}console.log(JSON.stringify({changed}))}finally{await mongoose.disconnect()}})();
