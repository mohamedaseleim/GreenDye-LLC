# GreenDye for Training and Consultancy V21

V21 resolves the verification and bulk credential API contract defects, replaces obsolete verification integration specifications, retains safe historical exports during credential migration, caches readiness checks, allows a bounded 48-hour ClamAV signature fallback, and removes unused frontend dependencies.

Local validation includes all acceptance gates V9 through V21, backend and frontend lint, backend unit tests, frontend tests, production build, bundle budget, syntax checks, lockfile installation, imports, and archive integrity. Connected staging must still execute the MongoDB replica-set integration suite, Docker builds and startup, process-user verification, EICAR, backup restoration, SMTP, security scans, load tests, and payment reconciliation.
