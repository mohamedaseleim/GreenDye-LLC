import apiClient from './apiClient';const api={get:path=>apiClient.get(`/consulting-operations${path}`),put:(path,data)=>apiClient.put(`/consulting-operations${path}`,data)};export const listProposals=()=>api.get('/proposals');export const decideProposal=(id,decision,notes)=>api.put(`/proposals/${id}/decision`,{decision,notes});export const listProjects=()=>api.get('/projects');export const getProject=id=>api.get(`/projects/${id}`);export default api;