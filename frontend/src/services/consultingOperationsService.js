import axios from 'axios';import {API_URL} from '../config';const api=axios.create({baseURL:`${API_URL}/api/consulting-operations`});api.interceptors.request.use(c=>{const t=localStorage.getItem('token');if(t)c.headers.Authorization=`Bearer ${t}`;return c});export const listProposals=()=>api.get('/proposals');export const decideProposal=(id,decision,notes)=>api.put(`/proposals/${id}/decision`,{decision,notes});export const listProjects=()=>api.get('/projects');export const getProject=id=>api.get(`/projects/${id}`);export default api;