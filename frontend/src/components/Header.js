import React from 'react';import {AppBar,Toolbar,Typography,Button,Box,Container} from '@mui/material';import {Link,useNavigate} from 'react-router-dom';import {useTranslation} from 'react-i18next';import {useAuth} from '../contexts/AuthContext';import {MOODLE_URL} from '../config';import {hasRole,isAdministrator} from '../utils/roles';import LanguageSwitcher from './LanguageSwitcher';
export default function Header(){const{t}=useTranslation();const{isAuthenticated,user,logout}=useAuth();const nav=useNavigate();return <AppBar position="static"><Container maxWidth="xl"><Toolbar sx={{gap:1,flexWrap:'wrap'}}><Typography variant="h6" component={Link} to="/" sx={{flexGrow:1,color:'inherit',textDecoration:'none',fontWeight:'bold'}}>🌿 GreenDye for Training and Consultancy</Typography><Box sx={{display:'flex',gap:1,alignItems:'center',flexWrap:'wrap'}}><Button color="inherit" component={Link} to="/services">{t('services')}</Button><Button color="inherit" component={Link} to="/request-consultation">{t('requestConsultation')}</Button><Button color="inherit" component={Link} to="/verify/trainer">{t('verifyTrainer')}</Button><Button color="inherit" component={Link} to="/verify/certificate">{t('verifyCertificate')}</Button><Button color="inherit" href={MOODLE_URL} target="_blank" rel="noopener noreferrer">{t('learningPlatform')}</Button>{isAuthenticated?<><Button color="inherit" component={Link} to={isAdministrator(user)?'/admin/dashboard':hasRole(user,'trainer')?'/trainer/dashboard':'/dashboard'}>{t('dashboard')}</Button><Button color="inherit" onClick={()=>{logout();nav('/')}}>{t('logout')}</Button></>:<Button color="inherit" component={Link} to="/login">{t('login')}</Button>}<LanguageSwitcher/></Box></Toolbar></Container></AppBar>}
